# Python
__pycache__/
*.py[oc]
.venv/
venv/
*.egg-info/
dist/
build/
wheels/

# Env / secrets. .envrc.local is replayed here rather than relying on the
# git-flow base's own copy: this overlay's .gitignore replaces the base's on
# layering (ADR-0020 in the template's source repo, project-starter-template).
.env
.envrc.local

# Manually-downloaded third-party index data (see
# src/golden_ratio_dual_gate/data/managed_futures.py) -- not ours to
# redistribute.
data/external/*.csv

# Editor / OS
.DS_Store
.vscode/
.idea/
